{
  "name": "@funicular/server",
  "version": "0.1.0",
  "private": true,
  "eslintConfig": {
    "extends": "../../package.json"
  },
  "scripts": {
    "dev": "dotnet watch",
    "build": "dotnet build",
    "build:js": "./esbuild.js",
    "restore": "dotnet restore",
    "lint": "dotnet csharpier . && dotnet format"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.10"
  }
}
